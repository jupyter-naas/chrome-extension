// setup react
import React from "react";
import ReactDOM from "react-dom/client";

// import the App component
import App from "./App";

// mount the App component on the root element
ReactDOM.createRoot(document.getElementById("root")).render(<App />);
